# 复制为 .env.local 后填写（Vercel 则在 Project Settings → Environment Variables 里填）
# 这些 Key 只在服务端使用，绝不会发送到浏览器。
#
# 不配置也能完整使用：覆盖看板走内置规则引擎，录音/分析会在页面内提示先配置。

# ===== 会议录音转写 API =====
TRANSCRIPTION_API_URL=
TRANSCRIPTION_API_KEY=
# 若你的转写服务还需要这些，取消注释并填写
# APP_ID=
# SECRET=

# ===== 大模型会议分析 API =====
LLM_API_URL=
LLM_API_KEY=
# 模型名（默认 deepseek-chat）
MODEL_NAME=deepseek-chat

# ===== 会中可选「AI 语义复核」（前端可临时自填，也可在此统一配置） =====
# AI_API_KEY=sk-xxxxxxxxxxxxxxxx
# AI_BASE_URL=https://api.deepseek.com/v1
# AI_MODEL=deepseek-chat
